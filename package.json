{
  "name": "mycodescan",
  "private": true,
  "version": "1.0.0",
  "description": "Barcode scanning companion for MyStockio. Scan on a phone, bill on a PC.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "api": "node server-reference/server.mjs",
    "test:api": "node server-reference/contract.test.mjs",
    "test": "npm run typecheck && npm run typecheck:test && npm run test:api && npm run test:queue && npm run test:sub && npm run test:arming",
    "test:sub": "esbuild src/lib/subbarcode.test.mts --bundle --format=esm --platform=node --outfile=.test-build/subbarcode.mjs --log-level=error && node .test-build/subbarcode.mjs",
    "test:queue": "esbuild src/lib/queue.test.mts --bundle --format=esm --platform=node --outfile=.test-build/queue.mjs --log-level=error --define:import.meta.env={\\\"VITE_API_ENV\\\":\\\"dev\\\",\\\"DEV\\\":true} && node server-reference/pin-base.mjs .test-build/queue.mjs http://127.0.0.1:8797/ && node .test-build/queue.mjs",
    "test:arming": "esbuild src/lib/arming.test.mts --bundle --format=esm --platform=node --outfile=.test-build/arming.mjs --log-level=error && node .test-build/arming.mjs",
    "typecheck:test": "tsc --noEmit -p tsconfig.test.json"
  },
  "dependencies": {
    "@zxing/browser": "^0.2.1",
    "lucide-react": "^0.451.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "@types/react": "^18.3.31",
    "@types/react-dom": "^18.3.7",
    "@vitejs/plugin-react": "^4.7.0",
    "autoprefixer": "^10.5.4",
    "postcss": "^8.5.26",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.9.3",
    "vite": "^5.4.21"
  }
}
